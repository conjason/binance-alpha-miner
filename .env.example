# 复制为 .env 并填入真实值（.env 已在 .gitignore 中）
#
# ── 本地币安 Parquet 训练/回测：以下都不需要，直接跑 train_binance.py / backtest_binance.py 即可 ──
#
# ── Web UI 用币安数据(SYMBOL.parquet 无周期后缀)时的两个可选设置 ──
# 无周期后缀文件的默认周期(目录名带 _1m 等会优先按目录推断；此为兜底默认)
# KLINE_DEFAULT_TIMEFRAME=5m
# Web/train_file.py 训练只取最近 N 根 K 线(5m 全量 40 万+根极慢，建议 6万~12万；空=全量)
# TRAIN_MAX_BARS=80000
#
# 可选：Web 界面「AI 分析」通道的 Key（用户自带；本 fork 不再自动读取任何本机凭证）
# DEEPSEEK_API_KEY=sk-xxxx
# OPENAI_COMPAT_API_KEY=sk-xxxx
# OPENAI_COMPAT_BASE_URL=https://api.openai.com/v1
# OPENAI_COMPAT_MODEL=gpt-4o-mini
#
# ── 仅当使用 MT5 实时/实盘模块（execution/ live_trade.py 等，对币安无用）才需要 ──
# MT5_LOGIN=12345678
# MT5_PASSWORD=your_password_here
# MT5_SERVER=YourBroker-Demo
